# ✈️ Aerospace Predictive Maintenance

### Week 2 - Edunet AI/ML Internship Project

**Objective:**  
Predict if an aircraft engine will fail within a particular cycle, using time-series sensor data.

---

## 📋 Steps Performed
1. Imported and preprocessed aircraft sensor data  
2. Generated Remaining Useful Life (RUL) feature  
3. Created binary target variable (failure within 30 cycles)  
4. Normalized data using StandardScaler  
5. Trained a Random Forest Classifier  
6. Evaluated model and saved results to `report_summary.json`

---

## 🧮 Model Summary
- Model: RandomForestClassifier  
- Accuracy: ~96.8%  
- Features: Sensor and cycle data  
- Type: Binary Classification (Failure/No Failure)

---

## ⚙️ Requirements
```
pip install -r requirements.txt
```

---

## ▶️ Run the Code
```
python main.py
```

---

## 📁 Files Included
- `main.py` → Source code  
- `requirements.txt` → Libraries  
- `report_summary.json` → Model summary  
- `aircraft_PM.csv` → Dataset

- 
Below are a few EDA plots used to understand the dataset.  
Plots can be generated by running the code in the project notebook.

## 📊 Exploratory Data Analysis (EDA)

To understand the aircraft engine behavior before modeling, I performed basic Exploratory Data Analysis (EDA).  
This helps identify patterns and relationships among sensor variables.

---

### **1️⃣ RUL (Remaining Useful Life) Distribution**

**Code Used:**
```python
plt.hist(df["RUL"], bins=40)
plt.title("RUL Distribution")
plt.xlabel("RUL")
plt.ylabel("Frequency")
plt.show()
```

---

### **2️⃣ Sensor Reading Distribution (Sensor 1)**

**Code Used:**
```python
plt.hist(df["sensor_1"], bins=40)
plt.title("Sensor 1 Distribution")
plt.xlabel("Sensor 1 Value")
plt.ylabel("Frequency")
plt.show()
```

---

### **3️⃣ Correlation Heatmap**

**Code Used:**
```python
plt.figure(figsize=(12,8))
sns.heatmap(df.corr(), cmap="coolwarm")
plt.title("Correlation Heatmap of Engine Sensors")
plt.show()
```

---

